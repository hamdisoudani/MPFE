NEXT_PUBLIC_LANGGRAPH_URL=http://localhost:2024

# Supabase (browser realtime subscription to lesson changes written by the agent via MCP).
# Use the ANON key only — never expose the service-role key to the browser.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
